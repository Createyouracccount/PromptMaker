# ARCHITECTURE — 시스템 설계 (2026-08-11 초안)

## 전체 구조: 코어 엔진 1개 + 배포 어댑터 N개

"VSCode든 CMD든 Cursor든 동일하게" 요구를 만족시키는 유일한 구조는
**재작성 로직을 클라이언트와 완전히 분리**하는 것이다.

```
사용자 (VSCode / CLI / Cursor / …)
        │ raw prompt
        ▼
┌─ 배포 어댑터 (얇게) ───────────────────────────────┐
│ A1. 슬래시 커맨드·스킬  (/pm …)   ← Claude Code    │
│ A2. UserPromptSubmit 훅 (자동 모드) ← Claude Code │
│ A3. 독립 CLI (promptmaker "…")    ← 어디서나      │
│ A4. MCP 서버 (refine_prompt 도구) ← Cursor 등     │
└───────────────┬────────────────────────────────┘
                ▼
┌─ 코어 엔진 (promptmaker-core) ──────────────────┐
│ 1) 모델 감지: 대상 모델 ID 확인                    │
│ 2) 의도 분류: 수정/구축/조사/디버그/일반              │
│ 3) 컨텍스트 수집: cwd, git, 프로젝트 구조(가능시)     │
│ 4) 재작성: 메타프롬프트 = 모델 프로필 + 장르 템플릿     │
│    → LLM 호출(claude -p 또는 API)로 재작성 수행    │
│ 5) 출력: 재작성 프롬프트 (+ 무엇을 바꿨는지 요약)       │
└───────────────┬───────────────────────────────┘
                ▼
┌─ 모델 프로필 (데이터, profiles/*.md) ────────────────────┐
│ fable-5.md / opus-5.md / sonnet-5.md / haiku-4-5.md │
│ 각 파일: 해당 모델이 잘 반응하는 스타일 규칙                  │
└─────────────────────────────────────────────────────┘
```

## 핵심 설계 결정

### D1. 재작성은 LLM에게 시키고, "모델별 차이"는 데이터로 관리한다
규칙 기반 재작성(정규식·템플릿 치환)은 품질 한계가 명확하다.
재작성 자체는 저렴한 모델(예: Haiku)에게 메타프롬프트로 시키되,
메타프롬프트에 **모델 프로필 파일**을 주입한다. 프로필은 마크다운 데이터이므로
코드 수정 없이 개선·추가 가능 (severity1의 nudge-JSON 구조 벤치마킹).

모델 프로필에 들어갈 내용 (예: fable-5 vs opus-5 차이):
- 선호 구조: 산문 위주 vs 목록/헤더 구조
- 지시 강도: 명시적 단계 지시 필요 여부 vs 목표+제약만 주면 되는지
- 컨텍스트 제공 방식, 금지 패턴(과도한 역할극, 불필요한 CoT 강제 등)
- 각 모델의 공식 프롬프팅 가이드 요약 + 우리가 실측으로 확인한 사실

### D2. MVP는 A3(독립 CLI)부터 만든다
훅·플러그인·MCP는 전부 껍데기다. 코어 엔진 품질이 전부이므로,
가장 빨리 반복 실험할 수 있는 CLI로 시작해 골든셋으로 품질을 잡은 뒤 어댑터를 붙인다.

### D3. Claude Code 통합의 프롬프트 교체 불가 제약 대응
UserPromptSubmit 훅은 프롬프트를 교체할 수 없다(RESEARCH.md §4).
- **옵트인 경로(A1)**: 슬래시 커맨드/스킬 — 재작성 후 그 프롬프트로 수행. 완전한 통제. 기본 제공.
- **자동 경로(A2)**: 훅에서 additionalContext로 "재해석 지시" 주입. 원문은 남지만 실질 효과는 유사. 선택 제공.

### D4. 재작성 결과는 투명하게 보여준다
사용자가 배우는 것도 목표다("프롬프트 작성에 어려움을 겪는 사람들을 돕는다").
재작성 프롬프트와 함께 **무엇을 왜 바꿨는지 1-3줄 요약**을 항상 출력 →
쓰다 보면 사용자 스스로 프롬프트 실력이 늘도록.

### D5. 언어: Python (uv 기반 단일 패키지)
- 기존 프로젝트와 스택 통일, PyPI 배포 경험 재사용.
- LLM 호출은 1차로 `claude -p`(Claude Code headless, 별도 API 키 불필요) 사용,
  API 직접 호출은 옵션으로.

## 모델 감지 전략 (Phase 2에서 실측 확정)

우선순위: ① 사용자가 명시(`--model`) → ② 훅 입력 JSON에 모델 필드가 있으면 사용
→ ③ `claude config get model` / `.claude/settings.json` → ④ 기본값(프로필 중 최신 모델).

## 열린 질문

- Q1. 재작성기(메타프롬프트 실행) 모델로 Haiku가 충분한가? → 골든셋으로 Haiku vs Sonnet 비교.
- Q2. 대화 중간(맥락이 이미 쌓인 상태)의 프롬프트도 재작성 대상인가? → MVP는 대화 시작/독립 요청에 집중, 중간 프롬프트는 Phase 2에서.
- Q3. 재작성 전 사용자에게 되묻기(clarifying questions)를 언제 허용할 것인가? → MVP는 되묻지 않고 가정을 명시하는 방식, 이후 옵션화.
