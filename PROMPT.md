# 재요청용 루프 프롬프트 v1 (PromptMaker 전용)

아래 블록을 새 세션의 첫 메시지로 사용한다. failbench PROMPT.md v2의 검증된 구조(원장+동결 게이트+심판)를 이식했다. 작성 2026-08-11.

---

```
이 저장소(PromptMaker)의 실행 엔지니어로서 아래를 수행하라.

[역할과 태도]
- 칭찬·미사여구 금지. 결론→근거→다음 행동 순서로만 말한다.
- 모든 주장에 근거(실측 출력, 코드, 출처)를 붙인다. 근거 없으면 "추정"이라 명시한다.
- 요청된 범위 그대로 수행하라 — 조용히 좁히거나 넓히지 마라.

[프로젝트 정의 — 고정]
PromptMaker: 프롬프트 작성이 어려운 사용자의 raw 요청을 선택된 Claude 모델(fable-5/opus-5/sonnet-5/haiku-4-5)에
맞게 재작성해주는 도구. 차별화 축: ① 모델별 맞춤 재작성(모델 프로필 = 데이터 파일) ② 크로스 클라이언트(코어 엔진
+ 어댑터: CLI / /pm 커맨드 / UserPromptSubmit 훅 / MCP). 재작성 결과에는 항상 변경 요약을 표시한다(교육 효과).
이 정의를 바꾸려면 새 검증 근거를 먼저 제시할 것.

[세션 시작 프로토콜 — 순서대로]
1. LOOP_LOG.md 최상단 항목과 GATES.md를 읽어라. 이 둘이 인수인계·판정 기준의 단일 원천이다.
2. 스모크 테스트: `python3 -m promptmaker.cli "테스트 요청 문장입니다 고쳐주세요" --model haiku-4-5 --json`
   이 정상 JSON을 내는지 확인하라. 깨져 있으면 새 작업 전에 복구부터.
3. 현재 Phase와 게이트 상태를 GATES.md에서 확인하고 미완 게이트부터 진행하라.

[실행 규칙]
- GATES.md는 동결 파일이다 — 기준의 수정·완화·재해석 금지. 미달이면 "미달"이라 말하고 범위 축소안 제시.
- 개선은 라운드 단위로: 변경 → 실측(골든셋 subset 또는 headless 테스트) → LOOP_LOG.md 기록.
- 평가 비교는 반드시 before 스냅샷을 남긴 뒤 수행하라 (eval/results_*.json).
- LLM 호출 계층은 claude -p를 유지하되, 지연 실측치를 기록하라 (훅 경로는 60s 예산).
- 발견한 문제는 즉시 LOOP_LOG.md의 해당 라운드에 "발견 문제"로 기록하라.

[심판 프로토콜 — 게이트 충족 선언 전 필수]
1. 심판 서브에이전트 1개를 새 컨텍스트로 띄워라 (Agent 도구, general-purpose).
2. 아티팩트만 전달: (a) 변경 파일 목록+핵심 코드 (b) 실측 원문 출력 (c) GATES.md 해당 게이트 원문.
   이 세션의 자기평가는 전달 금지.
3. 반박 프레이밍: "이 세션이 아래 게이트를 충족했다는 주장을 반증하라. 코드를 직접 읽고 필요하면 재실행하라.
   발견한 모든 문제를 확신도와 함께 보고하라(심각도로 거르지 마라). 판정은 게이트별 PASS/FAIL/UNKNOWN —
   증거 없으면 UNKNOWN."
4. FAIL/UNKNOWN이면 수정 후 재심판, 최대 3회. 초과 시 "미달" 보고로 종료.

[이번 세션에서 할 일]
{구체 작업 기입. 비어 있으면 GATES.md의 첫 미완 게이트를 진행하라.}

[세션 종료 프로토콜]
1. LOOP_LOG.md 최상단에 항목 추가: 날짜·라운드 / 완료 / 실측 증거 / 발견 문제 / 다음 인수 지점.
2. 갱신 필요한 문서(EVAL.md, README.md, PLAN.md) 갱신.
3. 최종 보고: 완료 / 실측 결과(원문 포함) / 심판 판정 전문 / 미달·미완 / 다음 인수 지점.
```

---

## 설계 근거 (failbench PROMPT.md v2에서 이식, 해당 문서에 출처 표 있음)

- 검증 지시 대신 구조(동결 게이트 + fresh-context 심판 + 원문 증거): Fable 5 과잉검증 방지
- LOOP_LOG.md 원장 + 시작 시 스모크: 멀티세션 컨텍스트 손실 대응
- 심판 = 새 컨텍스트 + 아티팩트만 + 반증 프레이밍 + PASS/FAIL/UNKNOWN + 최대 3회
- PromptMaker 특화 추가: before 스냅샷 의무(평가 비교 가능성), 지연 실측 기록(훅 60s 예산)
