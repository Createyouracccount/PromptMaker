# GATES.md — 동결 판정 기준

이 파일은 동결 파일이다. 기준의 수정·완화·재해석을 세션이 스스로 하지 않는다.
변경은 사용자 승인으로만 한다. 미달이면 "미달"이라 보고하고 범위 축소안을 제시한다.

## Phase 1 게이트 (판정 완료 2026-08-11 ✅)

- [x] P1-1: 골든셋 20개 중 16개 이상 "better" 판정 → 실측 20/20
- [x] P1-2: 프로필 간 구조적 차이 입증 (fable-5 vs haiku-4-5) → 실측 4/5 + 경계 조건 문서화

## Phase 2 게이트 (판정 완료 2026-08-11 ✅ — G2-1·2·4·5 심판 2차, G2-3 심판 3차)

- [x] **G2-1 모델 자동 감지**: UserPromptSubmit 훅 입력 JSON에 현재 모델 정보가 포함되는지 실측으로 확정하고, 감지 우선순위(명시 인자 → 훅 입력 → settings.json → 기본값)를 코드로 구현. 증거: 훅 입력 JSON 원문 캡처.
- [x] **G2-2 /pm 슬래시 커맨드**: `/pm <원문>` 이 재작성을 거쳐 수행됨을 headless(`claude -p`)에서 확인. 증거: 실행 원문 출력.
- [x] **G2-3 훅 자동 모드**: UserPromptSubmit 훅이 (a) 유효 JSON(additionalContext) 출력 (b) 단문(<10토큰)·`#raw` 태그 시 무개입 (c) 훅 타임아웃(30s) 내 완료. 증거: 훅 stdout 캡처 + 소요 시간.
- [x] **G2-4 Phase 1 백로그**: (a) claude 호출 파싱 실패 자동 재시도 구현 (b) fidelity 개선 — 재평가 subset(≥5)에서 "무단 세부 추가" 지적 감소 또는 fidelity 평균 ≥ 4.4. 증거: 재평가 결과 원문.
- [x] **G2-5 문서화**: LOOP_LOG.md에 라운드별 실측 증거 기록, 발견 문제는 EVAL.md 또는 LOOP_LOG.md에 남김.

## Phase 2 성공 선언 조건

전 게이트 충족 + fresh-context 심판(반증 프레이밍, 아티팩트만 전달)이 게이트별 PASS 판정.
심판 FAIL/UNKNOWN 시 수정 후 재심판, 최대 3회. 초과 시 "미달" 보고.
