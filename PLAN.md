# PLAN — 단계별 로드맵

각 Phase는 완료 기준(DoD)을 만족하고 LOG.md에 기록한 뒤 다음으로 넘어간다.
계획은 고정이 아니다 — 각 Phase 종료 시 확인된 사실로 다음 Phase를 재조정한다.

## Phase 0 — 조사·기획 ✅ (2026-08-11 완료)

- [x] 기존 유사 프로젝트 조사 (RESEARCH.md): 오픈소스 3종 + Anthropic 공식 도구 확인
- [x] 차별화 축 확정: ① 모델별 맞춤 재작성 ② 크로스 클라이언트
- [x] 기술 제약 확인: UserPromptSubmit 훅은 프롬프트 교체 불가
- [x] 아키텍처 초안 (ARCHITECTURE.md): 코어 엔진 + 어댑터 구조, MVP는 CLI부터

## Phase 1 — MVP: 코어 엔진 + 모델 프로필 ✅ (2026-08-11 완료)

결과: 골든셋 20/20 "better" (DoD ≥16), 프로필 간 차이 4/5 입증. 상세 EVAL.md.
잔여 개선 항목(fidelity, 재시도, opus/sonnet 프로필 변별력)은 Phase 2 착수 시 처리.

**목표**: `promptmaker "대충 쓴 요청" --model fable-5` → 재작성 프롬프트 + 변경 요약 출력.

1. 모델 프로필 작성 (`profiles/`): fable-5, opus-5, sonnet-5, haiku-4-5
   - 소스: 각 모델 공식 프롬프팅 가이드(platform.claude.com) + 실측
   - **프로필 간 차이가 실제 출력 차이를 만드는지가 핵심 검증 항목**
2. 코어 파이프라인: 의도 분류 → 프로필+장르 템플릿 메타프롬프트 → `claude -p`로 재작성
3. 골든셋 구축 (`golden/`): 실제로 어려움을 겪는 유형의 raw prompt 15~20개
   (너무 짧음 / 목표 불명 / 범위 없음 / 여러 요청 뒤섞임 / 한국어 구어체 등)
4. 평가: 골든셋 각 항목에 대해 (a) 재작성 품질 루브릭 채점, (b) 같은 raw를
   모델 프로필만 바꿔 재작성했을 때 유의미한 차이가 나는지 확인
5. DoD: 골든셋 20개 중 16개 이상에서 "원문보다 명확히 낫다" 판정 + 프로필 간 차이 입증

## Phase 2 — Claude Code 통합 ✅ 5/5 (2026-08-11, 심판 3라운드 검증)

결과: 전 게이트 PASS. 훅 지연은 메타프롬프트 축약(R7)으로 해소 — 재작성 14.5~19.2s, 28s 캡 fail-open.
상세는 LOOP_LOG.md R1~R7. 원 계획의 "지연 5초 이내" DoD는 claude -p 경로 한계로 미달성(직접 API 시 가능) — 게이트는 30s 기준으로 판정됨.

1. 슬래시 커맨드/스킬 `/pm`: 재작성 후 그 프롬프트로 바로 수행 (옵트인 기본)
2. UserPromptSubmit 훅 자동 모드(선택): additionalContext 재해석 주입, `#raw` 태그로 우회
3. 모델 자동 감지 실측: 훅 입력 JSON 필드 확인 → 감지 우선순위 확정 (ARCHITECTURE.md)
4. 지연·토큰 오버헤드 계측(재작성당 시간, 주입 토큰 수) — severity1 방식 벤치마킹
5. DoD: VSCode 확장 + 순수 CLI 양쪽에서 동일 동작 확인, 지연 5초 이내(Haiku 기준)

## Phase 3 — 크로스 클라이언트: MCP 서버 (예상 3~4일)

1. `refine_prompt(raw, target_model)` 도구를 노출하는 MCP 서버 (코어 엔진 재사용)
2. Cursor에서 등록·동작 검증
3. DoD: Claude Code 없이 Cursor 단독 환경에서 재작성 동작

## Phase 4 — 평가 루프·개선 (지속)

1. 사용 데이터 기반 개선: 재작성 수락/거부율, 재작성 후 되물음 감소 여부
2. 개인화: 사용자별 예시 라이브러리 (GaZmagik 벤치마킹)
3. 프로필 자동 검증: 골든셋 + 실제 태스크 성공률로 프로필 규칙 A/B (DSPy식 발상 차용)
4. 배포: PyPI + Claude Code 플러그인 마켓플레이스 등록

## 리스크

| 리스크 | 대응 |
|---|---|
| 모델별 차이가 생각보다 작아 차별화가 약함 | Phase 1의 프로필 간 차이 검증을 DoD에 포함. 차이가 작으면 "장르별 맞춤 + 크로스 클라이언트"로 축 이동 |
| 재작성 지연(수 초)로 UX 저하 | 옵트인 기본, Haiku 사용, 10토큰 미만 스킵(GaZmagik 벤치마킹) |
| 재작성이 사용자 의도를 왜곡 | 변경 요약 항상 표시(D4), 원문 보존, 자동 모드에서도 원문 유지(additionalContext 방식의 부수 이점) |
| 기존 오픈소스와 중복 | 차별화 축 2개 유지 + 기존 프로젝트 구조는 적극 벤치마킹(재발명 금지) |
